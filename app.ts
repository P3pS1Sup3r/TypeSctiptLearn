class User {
    name : string;
    constructor(_name) {
        this.name = _name;
    }
}

function add(a: number, b:number) {
    let result = a + b;
    console.log(result);
}
// Вызов функции
add(25,10);
add(25,15);